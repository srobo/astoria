Command Line Interface
======================

``astctl`` is the provided command line interface to Astoria. It is mainly intended for debugging and inspecting the state of Astoria.

For usage information, please see :ref:`command line usage`.

Every command in ``astctl`` is implemented as an individual :ref:`State Consumer <state consumers>`, and should be simple enough to understand without detailed documentation.